ALTER TABLE self_taught_tbl ADD COLUMN description STRING(500);
-- Set the default time zone to Asia/Tokyo.
-- only valid when no tables have been created yet.
-- ALTER DATABASE `self-taught` SET OPTIONS (default_time_zone = 'Asia/Tokyo');

